# Local Repo

A small demo repository used to practice Git workflows locally — staging,
committing, branching, and pushing to a remote. The page itself is a minimal
HTML/CSS example that adds a new paragraph each time a new feature is
"introduced" in a commit.
